{
  "name": "ots-teen-agent",
  "version": "0.1.0",
  "description": "Outsmart the System Money & Momentum Map — AI-guided self-discovery interview + scoring, with a teen-approved parent report.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "build:prompts": "node build-prompts.js",
    "test": "node test/run.js && node test/integration.js && node test/interview-deterministic.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.9",
    "nodemailer": "^9.0.3",
    "pg": "^8.13.1"
  }
}
